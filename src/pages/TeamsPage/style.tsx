import styled from "styled-components";

const ContainerTeams = styled.main`
    margin-top: 60px;
    padding: 0 120px;
`

const AllTeams = styled.section`
    display: flex;
    gap: 20px;
    flex-wrap: wrap;
`

export{
    ContainerTeams,
    AllTeams
}